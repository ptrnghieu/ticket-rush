const { Router } = require('express');

const router = Router();

// Seat routes are served under /api/events/:id/seats via event.routes.js.
// This stub exists for any standalone seat operations added later.

module.exports = router;
